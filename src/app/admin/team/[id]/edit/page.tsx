import { notFound, redirect } from "next/navigation"; import { isAuthenticated } from "@/lib/auth"; import { getTeamMember } from "@/lib/team-repo"; import AdminShell from "@/components/admin/AdminShell"; import TeamForm from "@/components/admin/TeamForm";
export const dynamic="force-dynamic";export default async function EditTeamPage({params}:{params:Promise<{id:string}>}){if(!(await isAuthenticated()))redirect("/admin/login");const member=await getTeamMember((await params).id);if(!member)notFound();return <AdminShell title="Edit Team Member" subtitle={`Update ${member.name}'s public profile.`}><TeamForm initial={member}/></AdminShell>}
